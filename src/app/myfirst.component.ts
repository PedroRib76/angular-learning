import { Component } from "@angular/core";

@Component({
    //tag HTML to call component
    selector: 'myfirst',
    templateUrl: './myfirst.component.html'
})
//When we need a class ou interface to be used outside the current file, use the EXPORT in front
export class MyFirstComponent{

}